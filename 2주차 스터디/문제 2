2번째 문제
1. a가 홀수면서 5보다 작으면 A를 출력합니다.
2. a가 홀수면서 5보다 같거나 크면 B를 출력합니다.
3. a가 짝수면 C를 출력합니다.

=============================================================
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

int main(void)
{
	int a;
	printf("a의 값 입력:");
	scanf("%d", &a);

	if (a % 2 != 0 && a < 5) //a가 홀수이면서 5보다 작을때
	{
		printf("A");
	}
	else if (a%2!=0 && a>=5) //a가 홀수이면서 5보다 같거나 클때
	{
		printf("B");
	}
	else if (a % 2 == 0) // a가 짝수 일때(조건식 대신 else를 써도 된다.)
	{
		printf("C");
	}
	return 0;
}
