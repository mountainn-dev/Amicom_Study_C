3번째 문제
아래와 같은 모양을 출력하는 프로그램은 어떻게 작성할 수 있을까요?
반복문을 이용해서 출력
Hint) 홀수 짝수번째
*
$
***
$
*****
$
=====================================================================================

#include<stdio.h>

int main(void)
{
	for (int i = 1; i < 7; i++) //홀수와 짝수 행을 의미하는 변수를 선언하지 않았기에 판별하기 쉽도록 i를 1로 설정, 6줄을 출력하므로 7보다 작을때까지 반복
	{
		if (i % 2 != 0) // 홀수행일 경우
		{
			for (int j = 0; j < i;j++) // i의 횟수 만큼 반복하게 된다.
			{
				printf("*");
			}
		}
		else // 짝수의 경우는 따로 연산자 표현을 하지 않았다.
		{
			printf("\n$\n");
		}
	}
	return 0;
}
